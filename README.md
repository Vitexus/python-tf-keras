# python-tf-keras (Vitexus packaging)

Debian packaging for the `tf-keras` PyPI package (legacy Keras 2 API,
kept standalone now that Keras 3 is TensorFlow's default backend).

Packaged as an opennsfw2 dependency for the invokeai-mastodon project's
local NSFW image classification.
